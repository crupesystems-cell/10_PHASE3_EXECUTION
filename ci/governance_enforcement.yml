name: Governance Hash Enforcement

on: [push, pull_request]

jobs:
  verify-governance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify frozen governance artifacts
        run: ./scripts/verify_governance_hashes.sh
