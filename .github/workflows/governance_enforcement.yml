name: Governance Enforcement (Frozen Artifacts)

on: [push, pull_request]

jobs:
  governance-hash-enforcement:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify frozen governance artifacts
        run: ./scripts/verify_governance_hashes.sh
